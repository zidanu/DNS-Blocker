# DNS-Blocker
A tool to prevent unwanted websites from connecting to your computer.

## Get Started
**Required:** C++11, Unix-like and POSIX compliant operating system
```bash
# Clone the repo
git clone github.com/zidanu/DNS-Blocker
cd DNS-Blocker
make
./dns_blocker /path/to/blocklist_file
```
